# Typography

字級階層與字重。字體為 system font stack（PingFang TC / Microsoft JhengHei …），整體字重 600、標題 700。

- 顯示級：`--text-display-lg`、`--text-display-md`（responsive `clamp`，用於 Hero / 大標）
- 內文級：`--text-xl`、`--text-lg`、`--text-base`、`--text-sm`
- 行高 `--line-height-base` 1.6。輔助文字用 `--color-text-muted`。
